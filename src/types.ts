// Interface for User Credentials

export interface UserCredentials {
    email: string;
    password: string;
}

export interface Environment {
    baseUrl: string;
    issuer: string;
    productName: string;
}

